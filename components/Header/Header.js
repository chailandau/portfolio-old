import Menu from "./Menu";

const Header = () => {
    return (
        <header>
            <div className="wrapper">
                <Menu />
            </div>
        </header>
    );
};

export default Header;
